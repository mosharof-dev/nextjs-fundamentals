import React from 'react';

export const metadata = {
  title: 'Profile',
  description: 'View detailed information about your profile.',
}

const ProfilePage = () => {
    return (
        <div>
            <h2>Profile</h2>
        </div>
    );
};

export default ProfilePage;